# NU:TONIC — Repo state vs target architecture (gap analysis)

**Date:** 2026-04-13  
**Reassessment:** 2026-04-13 (workspace reverification; supersedes gap table rows that assumed no `server/` and template KMP identity). **v0.5:** 2026-04-14 — client–server REST slice + **IMP-071** deep-link stub (see backlog §0.1). **v0.6:** 2026-04-14 — **IMP-060** community **`LeaderboardStore`** on **SQLite** (SQLAlchemy, `NUTONIC_LEADERBOARD_DATABASE_URL`, file default + Docker `/app/data`, tests + thread-safe engine). **v0.7:** 2026-04-14 — **IMP-080+** client manifest models + persistence (all targets), **`AiGuessStore`** + **IMP-083** local leaderboard write when gameplay receives repos from **`NutonicApp`**; SCAN hub **manifest-first** catalog load; server **public manifest redaction** (`NUTONIC_EXPOSE_MANIFEST_ROUND_TRUTH`), safer **feature defaults**, stricter JSON client parse, **If-None-Match** list handling; prose **`/api/v1/...`** path normalization in key `docs/`. **v0.8:** 2026-04-13 — **Completeness pass:** reconciled §1 table version label with v0.7 body; verified backlog “next wave” (**IMP-080–084**) vs `nutonic/` + `server/`; **IMP-080** / client **IMP-072** manifest path **landed**; **IMP-083** / **IMP-084** **partial** (see §2 **Next**); **IMP-081** still **open** (no versioned bundle artifact pipeline).  
**v0.9:** 2026-04-14 — **Shipped cache / narrative / hints:** normative **script + Gradle + embed** pipeline for per-map stills, coordinate-tier hints, optional SV+LFM packs, narrative bundles, **ranked clue slices** (golden withheld on device) — [`plans/2026-04-14-shipped-cache-narrative-hint-pipeline.md`](2026-04-14-shipped-cache-narrative-hint-pipeline.md). §1 **inference** row and §3 **streetview** row corrected (**stub** present); **IMP-081**–**083** sequencing tied to that plan in backlog **§0.1** (**v0.7**).  
**v1.0:** 2026-04-14 — **Inference:** `inference/lfm_vl_hint_service/` and `inference/lfm_vl_satellite_caption_service/` are **present** (pytest in CI), not absent. **Ranked / SCAN:** `streetview_hint_pack` is on **`docs/openapi.yaml`**, Kotlin DTOs, **`assemble_manifest`**, server **`RankedClueOut`** + static catalog, bundled **`ranked_clue_pack.json`** merge on client, and **fail-closed** world-map gameplay when no manifest row exists for a non-ranked map.  
**v1.1:** 2026-04-14 — **Docs accuracy:** **`inference/README.md`** status paragraph aligned with CI (**satellite caption** package not “planned-only”). **Shipped cache plan §4** + this file’s §2 **Next**: **`:shared:validateCatalog`** (**`validate_shipped_compose_resources.py`**) and **`sync_server_catalog.py`** (**codegen**) are **landed**; **`sync_server_catalog --mode sql`** (**IMP-120**) and broader **IMP-081** bundle-registry polish remain. **Streetview** preferred HTTP path is **`POST /api/v1/panos/sample`** (legacy **`/v1/panos/sample`** retained).  
**v1.2:** 2026-04-16 — **Table refresh:** **`IMP-090`** ranked round **start/submit** + SQLite store **landed** in `server/`; **`InferenceClient`** (**IMP-092**) **landed** for **PRO** upstream health + HMAC + optional **`POST …/internal/v1/materialize`** forward — remaining **IMP-092** work is **full multi-worker** orchestration / circuit breaker vs **TOPOLOGY** doc expansion. **CI:** **`nutonic-ci.yml`** includes **`desktop-msi`** (**windows-latest** + WiX) and **`desktop-dmg`** (**macos-latest**) in addition to **`.deb`** / **web JS** / **iOS** framework. **Claims memo §2.8 / §3** inference “absent” wording **superseded** (see **`plans/2026-04-13-claims-verification-baseline.md`** **v0.9**).  
**v1.3:** 2026-04-18 — **IMP-110 planning alignment:** normative WBS filed as [`plans/2026-04-18-streetview-google-perpendicular-sampling-full-scope.md`](2026-04-18-streetview-google-perpendicular-sampling-full-scope.md) (initial **v0.1–0.2** text mentioned road-perpendicular work; **v0.3** pivots DoD — see **v1.5** for repo cross-check).  
**v1.4:** 2026-04-20 — **Doc accuracy:** §3 **`inference/pro_materialization_service/`** row corrected (**present**, aligned with **`inference/README.md`**); §1 **`inference/*`** + **client shell** rows updated (PRO worker listed; gameplay entry **calls `refreshManifest()`** when **`ContentCacheRepository`** is wired — remaining gap is **payload** / embedded bundles vs redacted HTTP, not absence of refresh). §3 **conclusion** no longer lists **IMP-090** as “ahead” (server ranked core **landed** in v1.2).  
**v1.5:** 2026-04-20 — **IMP-110** reassessment vs **[`plans/2026-04-18-streetview-google-perpendicular-sampling-full-scope.md`](2026-04-18-streetview-google-perpendicular-sampling-full-scope.md) v0.3:** stochastic **`STOCHASTIC_S2_FOOTPRINT`** + **`pano=`** + random headings + legacy/omni paths **landed** in **`inference/streetview_pano_service/google_sample.py`**; batch forwards sampling args (**`tools/batch_streetview_hints.py`**). §1/§3 **streetview** rows stop treating **road-bearing** as DoD. **IMP-051:** bundled **silence `.wav`** placeholders under **`composeResources/files/music/`** (replace with mastered loops). **IMP-083:** **`WorldMapGameplayPersistenceTest`** documents a minimal desktop E2E against **`poi_0000`**. §2 **Next** adds **prioritized implementation targets** (post-verification). Claims memo **v1.2** cross-ref.  
**Audience:** Implementers prioritizing first milestones.  
**Target reference:** `plans/2026-04-07-complete-implementation-architecture.md` §2 (layout), §9 (phases).

---

## 1. Summary table

| Area | Target (normative) | Current repo state (**v1.5** · planning cross-check 2026-04-20) | Gap severity |
|------|---------------------|----------------------------------------------|--------------|
| Gradle root identity | `nutonic/` as KMP root; product package `com.nutonic.*` (`rules/03`, complete plan **C0**) | `rootProject.name = "nutonic"` in `nutonic/settings.gradle.kts`; Kotlin sources under `com.nutonic.*` (e.g. `shared`, `androidApp`, `mapview-desktop`) | **Low** — C0 identity landed; optional cleanup of legacy **photo gallery** sample paths (e.g. SETUP → “legacy gallery”) remains product choice |
| Thin game `server/` | FastAPI, `docs/openapi.yaml`, Dockerfile (`plans/2026-04-07-game-server-thin-orchestrator.md`) | **`server/`** present: `pyproject.toml` (no `torch` / TerraTorch), `src/nutonic_server/main.py`, `Dockerfile` (7860), `README.md`, **`server/docs/TOPOLOGY.md`**, `tests/`; **IMP-060:** **`LeaderboardStore`** protocol + **`SqliteLeaderboardStore`**; **IMP-090:** ranked **start/submit/forfeit** + SQLite **`ranked_store`**; **IMP-092 (partial):** **`InferenceClient`** health probes + optional HMAC + **`POST …/pro/jobs`** forward to **`pro_materialization_service`** when URLs set | **Low** — optional **JWT refresh-token** table (**IMP-060** backlog) and **full** orchestrator hardening (multi-worker URLs in **`TOPOLOGY`**, circuit breaker) remain |
| OpenAPI artifact | Versioned **`/api/v1/*`**, **`GET /api/v1/health`** (`rules/05`, backlog invariants) | **`docs/openapi.yaml`**: paths are full `/api/v1/...` (origin-only `servers[0].url`); health, config, auth, debug session, **`GET /api/v1/maps`** **200** (minimal catalog schema), leaderboard **GET/POST**, schemas for `features` / idempotency / **403** `feature_disabled` | **Low** — keep hand-maintained file in lockstep with FastAPI until export automation (**IMP-011** optional) |
| `inference/*` workers | Discrete services per `inference/README.md` | **`inference/README.md`** + **`streetview_pano_service/`** (health, **`POST /api/v1/panos/sample`** preferred; legacy **`POST /v1/panos/sample`**, **`GET /api/v1/pano/metadata`**) — **Google path:** **`google_sample.sample_panos_google_stochastic`** (seeded disk anchors, **`pano=`** + random headings), **`LEGACY_RADIAL_OFFSET`**, **`OMNI_SINGLE_PANO`**; stub/synthetic frames when unkeyed + **`lfm_vl_hint_service/`** (stub/transformers/OpenAI) + **`lfm_vl_satellite_caption_service/`** + **`pro_materialization_service/`** (internal materialize + stub route, optional HMAC; **`IMP-113`** polish per README); **`tools/batch_streetview_hints.py`** forwards **`sampling_mode`** / **`area_radius_m`** / seeds; optional **`terramind_tim_local/`**. **Normative DoD** for pano sampling: **[`plans/2026-04-18-streetview-google-perpendicular-sampling-full-scope.md`](2026-04-18-streetview-google-perpendicular-sampling-full-scope.md) §13** — **§13.1–§13.3** implemented in tree; **§13.4** (batch/docs/**`model_pins`** / LFM merge) + **§14 J*** optional items remain. Road-bearing / Tile graph (**plan §0** deferred) is **not** a ship gate. | **Medium–Low** — **IMP-113** stress matrix + **IMP-110** §13.4/§14 polish |
| Client shell (5 tabs, theme) | **C1** + **C2** (`NutonicTheme`, SCAN elevated, checklist shells) | **`NutonicTheme`**, **five tabs**, **`NutonicApp`** (splash → role → optional auth → shell), **`NutonicRoute` / `ShellDetail`** placeholders; **`NutonicApiClient`** (Ktor + OpenAPI DTOs, **`IMP-070`**) wired from app hosts; **SCAN** hub **`scanHubRefreshCatalog`**: **manifest-first** when **`ContentCacheRepository`** returns map rows, else **`GET /api/v1/maps`**; shared **`mapContextId`** with **RANK**; **world map gameplay** receives **`contentCacheRepository`** + **`LocalNonRankedLeaderboardRepository`** (**submit** sets **`roundInstanceId`**, **`LocalNonRankedLeaderboardRepository.appendRow`** on lock-in — **`IMP-083`** core); **`WorldMapGameplayDetail`** (when repo non-null) calls **`refreshManifest()`** then **`cachedDocument()`** on entry — same app-root **`ContentCacheRepository`** as SCAN; **Final results** composable shows **last local row** summary + **→ RANK** (**`IMP-071` / `IMP-084`** partial); global **music master** + **`resolveNutonicBgmTrack`** / **`PlatformBgmPlayer`** — **IMP-051 partial:** **`composeResources/files/music/*.wav`** ship as **minimal silence** placeholders (decoder/loop wiring); **replace** with mastered loops per **`docs/SCREEN-MUSIC-SPEC.md`** | **Medium** — **`MapViewport`** landed (**`IMP-073`**). **Remaining:** **IMP-081** bundle bytes/versioned package; **IMP-083** broader acceptance (optional **`guesses/record`**, Android/iOS E2E parity, full **`docs/GAME-ENGINE.md` §10** polish — desktop **`WorldMapGameplayPersistenceTest`** already covers manifest **`poi_0000`** slice); **IMP-084** **Success overlay** + **score share** stubs; **embedded** full local manifest + ranked clue pack vs **redacted** HTTP-only payloads (**shipped-cache plan**) |
| `server/docs/TOPOLOGY.md` | Env URLs, timeouts (`game-server-thin-orchestrator` §3) | **Present** (partial): invariants + **IMP-092** notes (**`InferenceClient`** timeouts, PRO probe behavior); per-service **`STREETVIEW_PANO_SERVICE_URL`** etc. still **TBD for server-orchestrated** batch (scripts/Jobs call workers today) | **Low** — expand when game server **proxies** or schedules batch against **`inference/*`** beyond PRO |
| CI / quality | `rules/11`, `docs/PM2_LOCAL_VERIFICATION.md` | **`.github/workflows/nutonic-ci.yml`**: **`nutonic/**`** → `./gradlew quality test`; **`desktop-deb`**, **`desktop-msi`** (WiX on **windows-latest**), **`desktop-dmg`** (**macos-latest**); **`web-bundles`** → **`:webApp:jsBrowserProductionWebpack`** (JS only — no **wasmJs** target in repo); **`server/**`** → **`pytest`**; path filters include **`docs/openapi.yaml`**; plus `security-codeql-and-secrets.yml` | **Low** — **PM2** local verification still **manual** per `docs/PM2_LOCAL_VERIFICATION.md` |

---

## 2. Kotlin multiplatform (`nutonic/`)

**Present modules (from `settings.gradle.kts`):** `:androidApp`, `:shared`, `:webApp`, `:desktopApp`, `:mapview-desktop`.

**Implication:** **`mapview-desktop`** remains the natural desktop **`MapViewport`** engine anchor per `docs/map-engines.md` / **IMP-000**. **Landed:** **IMP-070**; **IMP-060** (SQLite **`LeaderboardStore`**); **IMP-080** core (**`ContentCacheRepository`**, **`GET /api/v1/cache/manifest`**, ETag/304, **`ContentCacheRepositoryTest`**, per-target **`ManifestBlobStore`**); **IMP-072** client path **manifest-first** when cache returns maps; server catalog still **static** Python lists (**DB-backed catalog** still ahead). **Partial:** **IMP-071** (deep link + REST + final-results summary — release/demo hygiene still in wave table); **IMP-083** (local write + manifest-driven round when cached doc includes **`locations`** — default server **redacts** `locations`/`ai_guesses`; dev uses **`NUTONIC_EXPOSE_MANIFEST_ROUND_TRUTH=true`** or stale cached full envelope); **IMP-084** (final results partial; success overlay still placeholder-level). **Next (verified):** **`plans/2026-04-14-shipped-cache-narrative-hint-pipeline.md`** — **catalog + scripts + embedded manifests** (non-ranked **full** local cache; ranked **clue pack** without golden on disk) to close **IMP-081** / strengthen **IMP-082** / unblock **IMP-083** exit. **2026-04-14:** embedded **`manifest.full.json`** merge + **`files/ranked/ranked_clue_pack.json`** merge for ranked assists, **fail-closed** gameplay without catalog rows, and server **`streetview_hint_pack`** on **`RankedClueOut`** reduce the redaction / wire-thin **footgun**. **Landed the same wave:** **Gradle `:shared:validateCatalog`** (**`data/scripts/validate_shipped_compose_resources.py`**) and **`data/scripts/sync_server_catalog.py`** (**codegen** → **`catalog_generated.py`**). **Still backlog:** **`sync_server_catalog --mode sql`** (**IMP-120**), narrative **`prompts/`** tree + live **`narrative_llm_batch`**, and **IMP-081** automation polish (versioned bundle registry beyond manifest + still validation). Then **IMP-083** acceptance: broaden **automated E2E** (e.g. **`androidTest`** parity), optional telemetry **`POST`**, fuller state machine per **`docs/GAME-ENGINE.md` §10**; **IMP-084** success overlay + game **share** hooks. **IMP-090** / **W8** per backlog dependency diagram. Optional **IMP-060** refresh-token table remains backlog-only.

**Prioritized implementation targets (2026-04-20, after doc reverification):** (1) **IMP-081** — Mapbox still render script → **`composeResources/files/maps/`** + bundle index + **`assemble_manifest`** / **`sync_server_catalog --write`** hygiene per **shipped-cache plan §5**. (2) **IMP-110 §13.4** — assert **`model_pins`** + LFM caption rank merge in **`batch_streetview_hints.py`** vs **SPEC-batch-streetview-hints**; expand **`TOPOLOGY.md`** env examples for local pano + hint URLs. (3) **IMP-084** / **IMP-051 polish** — score share stub + crossfade + swap silence WAVs for licensed loops. (4) **IMP-092** — circuit breaker + multi-worker URL table when second inference origin ships.

---

## 3. Backend and inference

| Path | Expected | Actual (reverified 2026-04-20 · gap **v1.5**) |
|------|----------|---------------------|
| `server/src/nutonic_server/` | Thin orchestrator layout §3 | **Present** — same as v0.6 row **plus** **`GET /api/v1/cache/manifest`** with **ETag**, comma-separated **`If-None-Match`** (**`test_cache_manifest_if_none_match_accepts_comma_separated_list`**), default **redaction** of `locations` + `ai_guesses` (**`NUTONIC_EXPOSE_MANIFEST_ROUND_TRUTH`**, default **false**; **`test_cache_manifest_redacts_round_fixtures_by_default`**); **`Settings`**: safer defaults (**`feature_ranked`**, **`feature_pro_jobs`** false; **`feature_community_lb_post`** false unless env enables); **`GET /api/v1/maps`** **200** (static **`MapSummary`**); leaderboard **GET/POST** + **IMP-060** store |
| `inference/streetview_pano_service/` | CPU pano / static URL builder | **Present** — `FastAPI`, **`GET /health`**, **`POST /api/v1/panos/sample`** (legacy **`POST /v1/panos/sample`**), optional Google Static path; **`GET /api/v1/pano/metadata`**. **Core sampling (plan §13.1–§13.3):** **`STOCHASTIC_S2_FOOTPRINT`** with seeded anchors + random headings + **`pano=`** when metadata returns an id; **`LEGACY_RADIAL_OFFSET`**; **`OMNI_SINGLE_PANO`**; stub parity. **Remaining:** plan **§13.4** (batch/docs/**`model_pins`** / LFM rank merge) + **§14 J*** optional checklist — see **[`plans/2026-04-18-streetview-google-perpendicular-sampling-full-scope.md`](2026-04-18-streetview-google-perpendicular-sampling-full-scope.md) v0.3** (**road-bearing / Tile graph explicitly deferred, not DoD**). |
| `inference/lfm_vl_hint_service/` | GPU hint JSON | **Present** — `/health`, **`POST /v1/suggestions/from_frames`**, optional **`POST /v1/narrative/fuse`** (stub/transformers/OpenAI). |
| `inference/lfm_vl_satellite_caption_service/` | Satellite specialist | **Present** — **`POST /v1/infer`** (`task=caption`) for Mapbox still bytes in batch jobs. |
| `inference/pro_materialization_service/` | PRO fetch/resample | **Present** — **`inference/README.md`**: **`GET /health`**, **`GET /internal/v1/healthz`**, **`POST /internal/v1/materialize`** (Mapbox + VLM export; optional **S2L2A** / **`RGB_mapbox`** TiM NPZ with **`pip install .[s2]`**); stub **`POST /api/v1/materialize/stub`**; optional inbound HMAC (same pattern as **`streetview_pano_service`**); **`pytest`** in **`nutonic-ci.yml`**. **IMP-113** byte caps / **`FULL_STAC`** stress and extra VLM roles remain per package README. |

**Conclusion:** Game-server **P0 + S0-shaped** slice is **landed**; **S1c-shaped** catalog is **landed** via **`GET /api/v1/maps`** and **`GET /api/v1/cache/manifest`** (maps slice always present; **round fixtures** in manifest are **redacted by default** — set **`NUTONIC_EXPOSE_MANIFEST_ROUND_TRUTH=true`** for CI/dev tests that assert **`locations`** / **`ai_guesses`**). **IMP-060** is **landed**. Client **IMP-080** / manifest-first **IMP-072** path is **landed**; **IMP-083** local persistence is **landed** in app wiring with the **caveat** above when round truth is redacted server-side — **store builds** should ship **embedded full local manifest + ranked clue pack** per **`plans/2026-04-14-shipped-cache-narrative-hint-pipeline.md`** so gameplay does not depend on redacted HTTP alone. **Inference** follow-ups: **IMP-110** plan **§13.4** / **§14** (batch observability, docs, optional checklist) and **IMP-113** materialization stress matrix; **IMP-081** automation (**scripted** still registry + compose embed) remains the primary **SCAN** asset gap. **IMP-090** ranked server **start/submit/forfeit** is **landed** (v1.2); backlog **client ranked** / pack UX polish and OpenAPI bump discipline remain per **`plans/2026-04-13-prioritized-implementation-task-backlog.md` §0.1**. Sequencing: **shipped-cache plan** (catalog/scripts/embed) ∥ backlog **§0.1** (bundle → E2E **IMP-083** exit; **IMP-090** server core already merged — keep client + contract tests aligned).

---

## 4. Documentation completeness (informational)

Product specs under `docs/` remain **authoritative for behavior**. This gap analysis does **not** judge narrative doc freshness beyond **OpenAPI ↔ FastAPI** alignment (implementers should diff `docs/openapi.yaml` vs `server/src/nutonic_server/` when touching either).

---

## 5. Document history

| Version | Date | Notes |
|---------|------|-------|
| 0.1 | 2026-04-13 | Initial gap table from workspace glob + settings scan |
| 0.2 | 2026-04-13 | OpenAPI row: explicit **`/api/v1/health`** + versioned paths |
| 0.3 | 2026-04-13 | **Reverification:** `server/`, OpenAPI, `com.nutonic`, TOPOLOGY, client shell/navigation; CI scope note; inference still absent |
| 0.4 | 2026-04-13 | **Contract hardening:** `docs/openapi.yaml` uses origin-only `servers[0].url` + full `/api/v1/...` path keys (RFC 3986 join safety); `FEATURE_COMMUNITY_LB_*` → **403** `feature_disabled` on leaderboard routes; **`Idempotency-Key`** dedupe on **POST** leaderboard; **`server/tests`** asserts YAML parse + OpenAPI ↔ FastAPI route parity |
| 0.5 | 2026-04-14 | **Client + CI reassessment:** **`NutonicApiClient`** (**IMP-070**); **IMP-071** stub (**`NutonicRoute.Shell.rankFocusMapId`**, saveable route token **`#`**, Final results → RANK, shared **`mapContextId`**); **`GET /api/v1/maps`** **200** (corrects v0.3–v0.4 “501” wording); **CI** includes **`server/`** pytest on **`nutonic-ci.yml`**; **IMP-073** / BGM / spine gaps unchanged |
| 0.6 | 2026-04-14 | **IMP-060:** **`LeaderboardStore`** **SQLite** persistence (`nutonic_server/leaderboard_store.py`), env **`NUTONIC_LEADERBOARD_DATABASE_URL`**, Docker **`/app/data`**, **`server/.gitignore`** `data/`; §1–§3 and **Next** sequencing updated (persistence no longer blocking **IMP-072** soft gate for file-backed deploys) |
| 0.7 | 2026-04-14 | **IMP-080+083 wiring:** gameplay + final results consume app-root cache + local LB repos; SCAN manifest-first catalog; server manifest redaction + safer feature defaults + comma **`If-None-Match`**; client **`NutonicJson`** strict; **`/api/v1/`** doc path cleanup |
| 0.8 | 2026-04-13 | **Completeness verification:** §1 column **v0.8**; §2 **Next** aligned with repo (IMP-080/072 landed; IMP-081 open; IMP-083/084 partial; redaction caveat); §3 server table + conclusion refreshed; cross-ref backlog **§0.1 v0.8** |
| 0.9 | 2026-04-14 | **Shipped cache plan** cross-link; §1 **inference** row: **streetview** stub present; §3 **streetview** row corrected; §2 **Next** + conclusion: embedded manifest / ranked clue pack vs redacted HTTP only; §1 column **v0.9**; backlog **§0.1 v0.7** |
| 1.0 | 2026-04-14 | **Inference:** `lfm_vl_hint_service` + `lfm_vl_satellite_caption_service` **present**; §1 table + §3 rows updated. **Client/server:** ranked **`streetview_hint_pack`**, bundled ranked merge, fail-closed gameplay. |
| 1.1 | 2026-04-14 | **v1.1** reassessment line; §1 **`inference/*`** row (**`/api/v1/panos/sample`**, **`terramind_tim_local`**); §2 **Next**: **`validateCatalog`** + **`sync_server_catalog`** codegen **landed**; §3 streetview row paths. |
| 1.2 | 2026-04-16 | **§1 table:** **`IMP-090`** / **`IMP-092`** landed; **CI** row documents **MSI/DMG** + **JS-only web**; **`TOPOLOGY`** row = partial not empty stub; header **v1.2** line + claims memo cross-ref. |
| 1.3 | 2026-04-18 | Header **v1.3**; §1 **inference** row + §3 **streetview** row + conclusion: cross-ref **`plans/2026-04-18-streetview-google-perpendicular-sampling-full-scope.md`** (**IMP-110** WBS). |
| 1.4 | 2026-04-20 | Header **v1.4**; §1 **`inference/*`** lists **`pro_materialization_service/`**; §1 **client shell** documents **`WorldMapGameplayDetail`** **`refreshManifest()`** on entry + embedded-bundle caveat; §3 **`pro_materialization_service/`** **Present** (was incorrectly **Missing**); conclusion: **IMP-090** landed, sequencing note vs backlog **§0.1**. |
| 1.5 | 2026-04-20 | Header **v1.5**; §1 table **v1.5**; **IMP-110** vs **streetview plan v0.3** (stochastic DoD in repo; road-bearing not a gate); **IMP-051** silence WAV placeholders; **IMP-083** **`WorldMapGameplayPersistenceTest`** note; §2 **Next** adds prioritized target list; §3 streetview + conclusion tightened; claims memo **v1.2** pointer. |
